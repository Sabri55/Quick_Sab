<Window x:Class="Quick_Sab.Views.PackageCompareWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Quick_Sab - Package compare"
        Width="780" Height="520" MinWidth="560" MinHeight="360"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI" FontSize="13">
    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,8">
            <TextBlock Foreground="#666" TextWrapping="Wrap">
                <Run Text="Source: " />
                <Hyperlink Click="SourceLink_Click" ToolTip="Open the source folder in Explorer"><Run x:Name="SourceLinkText" /></Hyperlink>
            </TextBlock>
            <TextBlock Foreground="#666" TextWrapping="Wrap" Margin="0,2,0,0">
                <Run Text="Target: " />
                <Hyperlink Click="TargetLink_Click" ToolTip="Open the target folder in Explorer"><Run x:Name="TargetLinkText" /></Hyperlink>
            </TextBlock>
        </StackPanel>

        <DataGrid Grid.Row="1" x:Name="ResultGrid" AutoGenerateColumns="False" CanUserAddRows="False"
                  HeadersVisibility="Column" GridLinesVisibility="Horizontal" RowHeight="26"
                  SelectionMode="Extended" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Update" Width="60">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <CheckBox IsChecked="{Binding Selected, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                      HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Package (source)" Binding="{Binding Name}" IsReadOnly="True" Width="*" />
                <DataGridTextColumn Header="Source version" Binding="{Binding SourceVersion}" IsReadOnly="True" Width="110" />
                <DataGridTextColumn Header="Target version" Binding="{Binding TargetVersion}" IsReadOnly="True" Width="110" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" IsReadOnly="True" Width="130">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Up to date">
                                    <Setter Property="Foreground" Value="#16A34A" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Different version">
                                    <Setter Property="Foreground" Value="#D97706" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="Missing in target">
                                    <Setter Property="Foreground" Value="#DC2626" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>

        <ProgressBar Grid.Row="2" x:Name="CopyProgress" Height="8" Margin="0,10,0,0"
                     Minimum="0" Visibility="Collapsed" />

        <Grid Grid.Row="3" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" x:Name="SummaryText" VerticalAlignment="Center" Foreground="#666"
                       TextWrapping="Wrap" Margin="0,0,10,0" />
            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="RefreshButton" Content="Refresh" Padding="14,6" Margin="0,0,8,0" Click="Refresh_Click" />
                <Button x:Name="UpdateButton" Content="Update selected" Padding="14,6" Margin="0,0,8,0" Click="UpdateSelected_Click" />
                <Button Content="Close" Padding="14,6" IsCancel="True" Click="Close_Click" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
